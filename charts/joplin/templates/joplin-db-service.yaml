apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-db
spec:
  ports:
    - name: "postgresql"
      port: 5432
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ .Chart.Name }}-db
status:
  loadBalancer: {}
