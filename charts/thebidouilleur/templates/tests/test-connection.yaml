apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "TheBidouilleur.xyz.fullname" . }}-test-connection"
  labels:
    {{- include "TheBidouilleur.xyz.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "TheBidouilleur.xyz.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
