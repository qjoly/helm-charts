apiVersion: v1
kind: Service
metadata:
  name: {{ include "TheBidouilleur.xyz.fullname" . }}
  labels:
    {{- include "TheBidouilleur.xyz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "TheBidouilleur.xyz.selectorLabels" . | nindent 4 }}
